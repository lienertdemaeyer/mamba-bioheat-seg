# Weights directory

`weights/` is for local model checkpoints downloaded from GitHub Releases (or other artifact stores).

- Do not commit `*.pth` files to Git.
- Update `weights/manifest.json` with release asset names + SHA256.

